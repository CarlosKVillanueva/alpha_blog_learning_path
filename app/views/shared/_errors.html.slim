- if obj.errors.any?
  .alert.alert-danger.alert-dismissible role= "alert"
    h4.alert-heading
      | The following errors prevented the #{obj.class.name.downcase} from been saved
    ul
      - obj.errors.full_messages.each do |msg|
        li= msg
